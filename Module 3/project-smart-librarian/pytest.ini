[pytest]
# Let tests import project modules from the repository root.
pythonpath = .
# Disable pytest's cache plugin to keep the workspace cleaner.
addopts = -p no:cacheprovider
# Ignore temporary cache folders generated during local runs.
norecursedirs = pytest-cache-files-* .pytest_tmp pytest_cache_local .pytest_cache
